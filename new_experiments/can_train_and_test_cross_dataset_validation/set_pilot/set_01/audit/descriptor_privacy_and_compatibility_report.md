# Descriptor Privacy and Compatibility

## Schema

Descriptor vector contains behavioural features only; no raw CAN IDs, payloads, filenames, or labels.

## Compatibility

Descriptor dimension matches local feature schema (vehicle-agnostic behavioural features).

## Option

Option B — train fleet GNN on CTT training scenarios (cross-dataset framework validation).
